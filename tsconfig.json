{
  // Базовый файл конфигурации, который связывает конфигурацию клиентской части (app) и серверной/утилит (node)
  "files": [],
  "references": [
    // Конфигурация для исходного кода самого React приложения (папка src)
    { "path": "./tsconfig.app.json" },
    // Конфигурация для конфигурационных файлов, которые выполняются в среде Node.js (например, vite.config.ts)
    { "path": "./tsconfig.node.json" }
  ]
}
