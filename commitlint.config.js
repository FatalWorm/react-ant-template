export default {
  // Наследуем строгие базовые правила стандарта Conventional Commits
  extends: ['@commitlint/config-conventional'],

  // Здесь мы можем переопределить или добавить собственные правила.
  // Структура правила: [уровень_строгости, 'always'/'never', значение]
  // Уровни строгости: 0 - выключено, 1 - предупреждение (warn), 2 - ошибка (error)
  rules: {
    // 1. Расширяем список допустимых типов коммитов
    // Добавляем 'wip' (черновик) и 'hotfix' (срочный патч) к стандартному списку
    'type-enum': [
      2,
      'always',
      [
        'feat',     // Новая фича
        'fix',      // Исправление бага
        'docs',     // Документация
        'style',    // Форматирование кода
        'refactor', // Рефакторинг (изменение структуры без изменения логики)
        'perf',     // Оптимизация производительности
        'test',     // Тесты
        'build',    // Сборка, зависимости (npm, vite)
        'ci',       // Настройки CI/CD (GitHub Actions, GitLab CI)
        'chore',    // Рутина, утилиты
        'revert',   // Откат коммита
        // --- Наши кастомные типы ---
        'wip',      // Work In Progress (в процессе работы, незаконченный код)
        'hotfix',   // Срочное исправление прямо на продакшене
      ],
    ],

    // 2. Требуем, чтобы заголовок (после двоеточия) всегда был в нижнем регистре
    // Например: "fix: bug" - пройдет, а "fix: Bug" - выдаст ошибку
    'subject-case': [2, 'always', 'lower-case'],

    // 3. Строго запрещаем ставить точку в конце заголовка
    // Например: "feat: add button." - выдаст ошибку
    'subject-full-stop': [2, 'never', '.'],

    // 4. Ограничиваем максимальную длину всего заголовка (тип + контекст + описание)
    // 100 символов — стандарт для удобного чтения git log в терминале
    'header-max-length': [2, 'always', 100],

    // 5. Пустое описание (subject) запрещено
    'subject-empty': [2, 'never'],

    // 6. Пустой тип (type) запрещен
    'type-empty': [2, 'never'],
  },
};
